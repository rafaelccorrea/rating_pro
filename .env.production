# Carregado pelo Vite (envDir = raiz) em builds de producao, sobrepondo o .env.
# Commitado de proposito: so contem valores publicos que ja vao para o bundle.
VITE_API_URL=https://mistyrose-lion-706980.hostingersite.com/api
